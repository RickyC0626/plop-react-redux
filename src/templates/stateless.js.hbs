{{#if useFlow}}
// @flow
{{/if}}
import React from 'react';
{{#if useReactIntl}}
import { FormattedMessage } from 'react-intl';
{{/if}}

import { style, Container } from './{{ properCase name }}.style';

const {{ properCase name }} = () => {
  return (
      <Container>
      {{#if useReactIntl}}
        <FormattedMessage id="" />
      {{/if}}
      </Container>
  );
};

export default {{ properCase name }};
